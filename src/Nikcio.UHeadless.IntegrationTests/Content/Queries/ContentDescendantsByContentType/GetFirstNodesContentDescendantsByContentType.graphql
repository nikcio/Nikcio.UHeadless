query GetFirstNodesContentDescendantsByContentType($firstCount: Int) {
    contentDescendantsByContentType(contentType: "default", first: $firstCount, order: { id: ASC}) { 
        nodes {
            id 
        }
        pageInfo {
          hasNextPage
        }
    }
}