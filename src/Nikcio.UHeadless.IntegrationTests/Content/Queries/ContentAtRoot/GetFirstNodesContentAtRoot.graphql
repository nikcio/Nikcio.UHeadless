query GetFirstNodesContentAtRoot($firstCount: Int!) {
    contentAtRoot(first: $firstCount, order: { id: ASC}) { 
        nodes {
            id 
        }
        pageInfo {
          hasNextPage
        }
    }
}