query GetFirstNodesContentByContentType($firstCount: Int!) {
    contentByContentType(contentType: "default", first: $firstCount, order: { id: ASC}) { 
        nodes {
            id 
        }
        pageInfo {
          hasNextPage
        }
    }
}