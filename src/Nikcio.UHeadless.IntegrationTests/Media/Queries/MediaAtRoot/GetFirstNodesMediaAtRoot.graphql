query GetFirstNodesMediaAtRoot($firstCount: Int!) {
    mediaAtRoot(first: $firstCount, order: { id: ASC}) { 
        nodes {
            id 
        }
        pageInfo {
          hasNextPage
        }
    }
}