{
  "version": "5.0.0-preview001",
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "scripts": {
    "release": "standard-version",
    "setup": "docker-compose -f ./setup/docker-compose.yml up -d",
    "graph": "dependensee ./src ./src/Dependencies/graph.html"
  },
  "standard-version": {
    "skip": {
      "commit": true,
      "tag": true
    }
  }
}
