{
  "version": "3.0.0-preview002",
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "scripts": {
    "release": "./release.bat && standard-version",
    "setup": "docker-compose -f ./setup/docker-compose.yml up -d"
  },
  "standard-version": {
    "skip": {
      "commit": true,
      "tag": true
    }
  }
}
