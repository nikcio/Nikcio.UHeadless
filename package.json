{
  "version": "2.3.1",
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "scripts": {
    "release": "standard-version && release.bat",
    "setup": "docker-compose -f ./setup/docker-compose.yml up -d"
  },
  "standard-version": {
    "skip": {
      "commit": true,
      "tag": true
    }
  }
}
